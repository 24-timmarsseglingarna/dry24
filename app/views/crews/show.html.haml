

.map-wrapper
  #map
  .outer-game-container
    .inner-game-container
      .game-box
        -#%p#notice= notice
        %h1= @crew.boat_name
        Skeppare:
        = @crew.captain_name
        %br
        Start/mål:
        = @crew.start_point.number_name
        = image_tag "https://chart.googleapis.com/chart?chst=d_map_spin&chld=0.6|000000|ff8c00|8|_|#{URI.encode(@crew.start_point.number)}"
        %br
        Snart framme vid:
        = @crew.last_point.number_name
        = image_tag "https://chart.googleapis.com/chart?chst=d_map_spin&chld=0.6|000000|66ff66|8|_|#{URI.encode(@crew.last_point.number)}"
        %br
        = "Tid #{I18n.l DateTime.now, :format => :long}"
        %br Vind AWA: -..° AWS .. m/s
        Kurs COG: ..°
        %br Fart VMG: ... knop VTS: ... knop
        %h3 Vart nu?
        %ol
        -for section in @sections do
          %li= link_to (raw(section.to_point.number_name) + ' (' + section.distance.to_s + ' M)'), create_log_entry_crew_url(:to_point_id => section.to_point.id), :method => :post

        %h3 Loggbok
        %table.logbook
          - for log_entry in @crew.log_entries.reverse do
            %tr
              %td= I18n.l log_entry.to_time, :format => :long
              %td= raw (log_entry.to_point.number_name)
              %td 5,6M
              %td NV
              %td 3 m/s

= render 'map'