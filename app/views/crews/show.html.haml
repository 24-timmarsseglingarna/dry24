%p#notice= notice

.map-wrapper
  #map
  %div{:class => "alpha60", :id => "boat-data"}
    %h1
      = @crew.boat_name
    %h2
      = @crew.captain_name
    %p
      %b Next leg
      %ul
      - for section in @crew.start_point.sections do
        %li= "#{section.point.number} - #{section.to_point.number_name} (#{section.distance} M)."

    %p

= render 'map'