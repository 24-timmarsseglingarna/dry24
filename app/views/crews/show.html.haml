

.map-wrapper
  #map
  #boat-data
    #crew
      %p#notice= notice
      %h1= @crew.boat_name
      %p
        Skeppare:
        = @crew.captain_name
      %p
        Nu vid:
        = raw @crew.last_point.number_name
        %br
        Nu är klockan ...
        %br
        Vind: NNV 4 m/s
        %br
        Hastighet: 3,5 knop
    #targets
      %b Vart nu?
      = semantic_form_for @log_entry, :url => create_log_entry_crew_url do |f|
        = f.input :to_point, :as => :radio, :label => 'Where to?', :collection => Hash[@point_options.map{|b| ["#{b.number} #{b.name}",b.id]}]
        -#= f.input :point
        -#= f.input :crew_id
        = f.actions do
          = f.action :submit, :as => :button, :label => 'Segla vidare'


        -# %ul
        -# - for section in @crew.last_point.sections do
        -#   %li= link_to "#{section.point.number} - #{section.to_point.number_name} (#{section.distance} M).", create_log_entry_crew_path(:section_id => section.id)

    #logg-book
      %p
        %b Loggbok
        %ul
        - for log_entry in @crew.log_entries do
          %li= raw (log_entry.point.number_name) + '--' + log_entry.to_point.number_name


= render 'map'